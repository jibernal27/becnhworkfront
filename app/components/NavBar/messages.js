import { defineMessages } from 'react-intl';
export const scope = 'header';
export default defineMessages({
  profile: {
    id: `${scope}.profile`,
  },
  places: {
    id: `${scope}.places`,
  },
  files: {
    id: `${scope}.files`,
  },
  user: {
    id: `${scope}.user`,
  },
  logout: {
    id: `${scope}.logout`,
  },
  logIn: {
    id: `${scope}.login`,
  },
  signUp: {
    id: `${scope}.signUp`,
  },
});
