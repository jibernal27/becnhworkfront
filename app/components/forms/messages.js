import { defineMessages } from 'react-intl';
export const scope = 'forms';
export default defineMessages({
  username: {
    id: `${scope}.username`,
  },
  firstName: {
    id: `${scope}.firstName`,
  },
  lastName: {
    id: `${scope}.lastName`,
  },
  profilePicture: {
    id: `${scope}.profilePicture`,
  },
  shortBiography: {
    id: `${scope}.shortBiography`,
  },
  password: {
    id: `${scope}.password`,
  },
  confirmPassword: {
    id: `${scope}.confirmPassword`,
  },
  language: {
    id: `${scope}.preferredLanguage`,
  },
  signUp: {
    id: `${scope}.signUp`,
  },
  required: {
    id: `${scope}.errors.required`,
  },
  maxLength: {
    id: `${scope}.errors.maxLength`,
    one: `${scope}.errors.maxLength.one`,
    other: `${scope}.errors.maxLength.many`,
  },
  minLength: {
    id: `${scope}.errors.minLength`,
    one: `${scope}.errors.minLength.one`,
    other: `${scope}.errors.minLength.many`,
  },
  samePassword: {
    id: `${scope}.errors.passwordConfirmation.different`,
  },
  login: {
    id: `${scope}.logIn`,
  },
  editProfile: {
    id: `${scope}.editProfile`,
  },
  name: {
    id: `${scope}.name`,
  },
  picture: {
    id: `${scope}.place.picture`,
  },
  dateInit: {
    id: `${scope}.place.dateInti`,
  },
  dateEnd: {
    id: `${scope}.place.dateEnd`,
  },
  create: {
    id: `labels.create`,
  },
  file: {
    id: `${scope}.file`,
  },
});
